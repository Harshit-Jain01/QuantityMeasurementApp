spring:
  datasource:
    url: jdbc:mysql://localhost:3306/quantityapp
    username: root
    password: abcd
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
